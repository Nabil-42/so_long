# Installation — so_long

## Requirements

- OS: Linux (MiniLibX X11) or macOS (MiniLibX Cocoa)
- gcc
- X11 development libraries (Linux): `sudo apt install libx11-dev libxext-dev`
- Make

## Build

```bash
git clone https://github.com/Nabil-42/so_long.git
cd so_long
make
```